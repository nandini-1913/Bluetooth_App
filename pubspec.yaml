name: rio_smart
description: "A new Flutter project for rio smart meter"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.34.4
  permission_handler: ^11.4.0
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8
  encrypt: ^5.0.3
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  excel: ^2.0.4
  fluttertoast: ^8.2.14


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true